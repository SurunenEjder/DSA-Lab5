apiVersion: 1
providers:
  - name: golden-signals      # appears in UI
    orgId: 1
    folder: ''                # import into root folder
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards